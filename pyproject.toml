[project]
name = "jarvis-tui"
version = "0.3.0"
description = "TUI do mordomo Jarvis — chat + voz conectados ao agente Jarvis via opencode"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
dependencies = [
  "textual>=3.0",
  "rich>=13.0",
]

[project.scripts]
jarvis-tui = "jarvis_tui:main"

[tool.uv]
package = false
